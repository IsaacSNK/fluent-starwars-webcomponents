import { fillColor, neutralFillLayerRest, neutralLayerFloating } from "@fluentui/web-components";
import { css } from "@microsoft/fast-element";

export const StarWarsCharacterStyles = css`
    .card {
        padding: 12px;
        background-color: ${neutralLayerFloating}
    }
`;